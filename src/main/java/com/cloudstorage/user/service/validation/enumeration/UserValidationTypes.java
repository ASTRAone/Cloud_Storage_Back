package com.cloudstorage.user.service.validation.enumeration;

import com.cloudstorage.common.validation.api.ValidationType;

public enum UserValidationTypes implements ValidationType {
    CREATE,
    UPDATE
}
